// app.js — связывает всё вместе

document.addEventListener('DOMContentLoaded', () => {
  console.log('Путевой лист загружен');

  // пример
  // fetch('/dj/api/points_summary?...')
  //   .then(r => r.json())
  //   .then(data => updateTotals(data));
});
